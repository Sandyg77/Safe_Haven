import { Card } from "react-bootstrap";

const Properties = () => {
  return (
    <Card style={{ width: "50rem" }}>
      <Card.Header>Available Properties 🏡</Card.Header>
      <Card.Body>
        This is my Properties Component and where the json data should be loaded
      </Card.Body>
    </Card>
  );
};

export default Properties;
